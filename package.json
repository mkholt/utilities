{
  "name": "@mkholt/utilities",
  "version": "1.0.1",
  "description": "Fully typed small utility functions that I often need",
  "repository": {
    "type": "git",
    "url": "github:mkholt/utilities"
  },
  "files": [
    "dist/*"
  ],
  "main": "dist/index.js",
  "author": "Morten Holt <mho@delegate.dk>",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "prepack": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
